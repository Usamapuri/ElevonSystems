# Handover — Elevon POS, end of Phase 0 (2026-09-20)

Read this first in a fresh session, then `CLAUDE.md`, then the spec and the plan it points to.

## Where things are

| Item | Location |
|---|---|
| Repo | `Usamapuri/ElevonSystems`, local checkout is this folder. Branch `dev` (15 commits ahead of `main`). **Nothing has been pushed to GitHub.** |
| Design spec | `docs/superpowers/specs/2026-09-19-elevon-lpg-pos-design.md` — §2 owner decisions, §5 data model, §6 behaviour, §7 FBR DI, §13 phases |
| Phase 0 plan (done) | `docs/superpowers/plans/2026-09-19-phase-0-scaffold.md` |
| Project rules | `CLAUDE.md` (isolation from Bhookly, invariants, commit format, test commands) |
| Source of copied patterns | `../POS-System-General` (Bhookly Retail). Sibling, not a fork. Copy selectively, rename `bhookly → elevon`. |

## What Phase 0 delivered

- **Backend** (`backend/`, module `elevon-backend`): `/health`; embedded ordered migrations in `backend/migrations/NNN_*.sql`, applied at boot, fatal on failure, recorded in `schema_migrations`; `001_init` creates `users` and `settings` and seeds default setting keys; `EnsureInitialAdmin` from `INITIAL_ADMIN_PASSWORD`; JWT (24 h, `X-POS-JWT` fallback, revocation check fails closed); `POST /api/v1/auth/login`, `GET /api/v1/auth/me`; `routes.go` with `staff` and `admin` groups, pinned by a contract test; `cmd/migrate`; `internal/testdb` harness (`TEST_DATABASE_URL`, skips loudly, CI fails on skip).
- **Frontend** (`frontend/`, `elevon-pos-frontend`): Vite 5, TS strict, Tailwind 3 with the retail theme tokens, vendored shadcn kit, `api/client.ts` (session keys `elevon_token` / `elevon_user`), `lib/roles.ts` (admin, counter, nav items, `canAccess`, `defaultPath`), `lib/print/transport.ts` (`window.elevon` bridge stub), theme context (`elevon-theme`), login page, `_app` layout with `beforeLoad` guards, sidebar, user menu, placeholder pages for `/pos /day-close /customers /invoices /dashboard /reports /rates /settings`.
- **Ops**: Dockerfiles (backend prod + air dev, frontend nginx), `railway.json` per service, `docker-compose.dev.yml`, `Makefile`, two GitHub Actions workflows, `.claude/settings.json` hiding the Bhookly tenant skills.

Verified: both suites green (backend incl. the DB-backed migration test on Postgres 16), both images build and run, the compose stack boots end to end (migrate → admin → login), and the browser flow (redirects, login, role landing, theme, logout) works.

## How to run it here

```bash
cp .env.example .env         # already exists locally with INITIAL_ADMIN_PASSWORD=correct-horse-battery (gitignored)
docker compose -f docker-compose.dev.yml up --build
```
The Bhookly Retail dev containers (`pos-backend-dev` on 8080, `pos-frontend-dev` on 3000) are usually running on this machine, so the local `.env` overrides the host ports: API `http://localhost:18080`, Vite `http://127.0.0.1:13000`, Postgres `15432`. Use `127.0.0.1`, not `localhost`, in the browser (localhost resolves to IPv6 first here).

Without Docker for the API: `cd backend && DATABASE_URL=... INITIAL_ADMIN_PASSWORD=... GIN_MODE=debug go run .`
Tests: `cd backend && go vet ./... && go test ./...` (set `TEST_DATABASE_URL` to a disposable DB to run the DB-backed tests) · `cd frontend && npm run type-check && npm run test`.

## Deviations and gotchas to carry forward

- **Route guards use `beforeLoad` + `redirect`, never `<Navigate>` in a layout render.** The installed TanStack Router resolved to 1.170 (retail pins 1.57); its `Navigate` re-fires on every render while a redirect is pending and looped on logout. Keep this pattern for every new route.
- `frontend/src/routeTree.gen.ts` is generated by the router plugin during `vite build` / `vite dev`; it is committed so `tsc` works, never hand-edited.
- Login/JWT/`/auth/me` were pulled forward from Phase 1 so the shell is usable. Still Phase 1: users CRUD, PIN set, forgot/reset/change password, the login rate limiter, settings API and the Settings screen sections.
- `debounce` in `lib/utils.ts` was changed to `ReturnType<typeof setTimeout>` (no `@types/node` here).
- Migrations must stay idempotent; `migrations_idempotent_contract_test.go` enforces the accepted forms (`IF NOT EXISTS`, `DROP CONSTRAINT IF EXISTS` before `ADD CONSTRAINT`, `ON CONFLICT` on seeds).
- The isolation grep (`grep -rni bhookly`) legitimately hits `CLAUDE.md` and `.claude/settings.json`, which name what to avoid. Anything else is a violation.

## Owner decisions still assumed (confirm when convenient)

- Brand name "Elevon POS" / "Elevon Systems" (constants in `index.html`, `login.tsx`, `Sidebar.tsx`).
- Gross − tare entry mode on the weight pad for cylinders (spec D2).
- Tax rates are seeded as 0 until the tax advisor's figures are entered; the LPG HS code `2711.1910` is a settings default to be confirmed, never a code default.

## Next step

Phase 1 (spec §13): brainstorm briefly if anything changed, then write `docs/superpowers/plans/<date>-phase-1-auth-users-settings.md` with the writing-plans skill and execute it on `dev`. Commit prefix `E-01`.

A fresh Claude session started in this folder gets its own memory directory; this file, `CLAUDE.md`, the spec and the plan are the full context.
